<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kotlin_proyect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="BUTTON_NUM">
        <item name="android:backgroundTint">@color/gray_black</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="BUTTON_SIGN">
        <item name="android:backgroundTint">@color/gray_black</item>
        <item name="android:textColor">@color/neon_green</item>
        <item name="android:textSize">40sp</item>
    </style>

    <style name="TEXT_EDITOR">
        <item name="android:background">@color/gray_black</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>